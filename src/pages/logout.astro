---
import { ROUTES } from "@/lib/routes";

if (Astro.cookies.has("Authorization")) {
  Astro.cookies.delete("Authorization", {
    path: "/",
  });
}
ROUTES; // NOTE: Dereferencing removes a warning
return Astro.redirect(ROUTES.LOGIN.url);
---
